<h1>Construct Your Own Dictionary!</h1>

  #if ($tasks)
    <ul>
      #foreach ($task in $tasks)
        <li>$task.getTasks()</li>
      #end
    </ul>
  #else
    <p>No task!</p>
  #end

<p>Add a new Vocabulary Task:</p>

<form action="/tasks" method="post">
  <label for="tasks">Vocabulary Task</label>
  <input id="tasks" name="tasks" type="text">

  <button type="submit" class="btn btn-default">Add Vocabulary Task</button>
</form>
